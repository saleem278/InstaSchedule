# Leave empty/unset for local dev — Vite's dev server proxies /api and /uploads
# to the backend (see vite.config.ts), so requests are same-origin.
# Set this to your backend's origin (e.g. https://api.example.com) for a
# production build served separately from the API.
VITE_API_BASE_URL=
